//Include libraries

int main(int argc, char** argv){
    int takes = 8;
    ints dis[takes];
    for(int i=0;i<takes;i++) {
	 rotate(360/((double)takes));
	 dis[i] = readDis();
    }
    int bst=0,chances=360;
    for(int i=0;i<chances;i++){
	 
    }
}
