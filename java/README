This is an example of an Java project, with an associated Ant build config.
You can add targets and expand on this to build your real Java project,
or write yours from scratch.

Commands:
-- ant jar: Compile and jar the Java files (subset of 'hello' command).
-- ant hello: Compile the Java files, then run the HelloWorld task.
-- ant clean: Deletes compiled files.